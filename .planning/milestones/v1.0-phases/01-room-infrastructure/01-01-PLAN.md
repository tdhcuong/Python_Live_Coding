---
phase: 01-room-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/requirements.txt
  - backend/app/__init__.py
  - backend/app/main.py
  - frontend/package.json
  - frontend/vite.config.js
  - frontend/index.html
  - frontend/src/main.js
  - frontend/src/style.css
autonomous: true
requirements:
  - ROOM-01
  - ROOM-02

must_haves:
  truths:
    - "Running `uvicorn app.main:app` from the backend/ directory starts a FastAPI server on port 8000 without errors"
    - "Running `npm run dev` from frontend/ starts Vite dev server on port 5173 without errors"
    - "Visiting http://localhost:5173 returns a dark-themed HTML page (not a Vite placeholder)"
    - "GET http://localhost:8000/ returns JSON {\"status\": \"ok\"}"
  artifacts:
    - path: "backend/requirements.txt"
      provides: "Python dependency declarations"
      contains: "fastapi"
    - path: "backend/app/main.py"
      provides: "FastAPI application entry point"
      exports: ["app"]
    - path: "frontend/package.json"
      provides: "Node project manifest"
      contains: "tailwindcss"
    - path: "frontend/vite.config.js"
      provides: "Vite build config with Tailwind v4 plugin"
      contains: "@tailwindcss/vite"
    - path: "frontend/src/style.css"
      provides: "Tailwind v4 import"
      contains: "@import \"tailwindcss\""
  key_links:
    - from: "frontend/vite.config.js"
      to: "@tailwindcss/vite"
      via: "plugins array"
      pattern: "tailwindcss\\(\\)"
    - from: "frontend/src/main.js"
      to: "frontend/src/style.css"
      via: "import statement"
      pattern: "import.*style.css"
---

<objective>
Bootstrap the project directory structure: a Python backend (FastAPI + Uvicorn) and a Vite + Tailwind CSS v4 frontend. No room logic yet — this plan creates the runnable skeleton that all subsequent plans build on.

Purpose: Every other plan assumes a working dev environment with FastAPI running on :8000 and Vite on :5173. This plan creates that foundation.
Output: Two runnable services (backend dev server, frontend dev server), a dark-themed index page, and a health check endpoint.
</objective>

<execution_context>
@/Users/tdhcuong/Desktop/Personal_Projects/Python_Live_Coding/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tdhcuong/Desktop/Personal_Projects/Python_Live_Coding/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/STACK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend scaffold — FastAPI project with health check</name>
  <files>backend/requirements.txt, backend/app/__init__.py, backend/app/main.py</files>
  <read_first>
    - .planning/research/STACK.md (FastAPI version, Uvicorn version, installation commands)
    - CLAUDE.md (project constraints and conventions)
  </read_first>
  <action>
Create the following directory structure and files from the project root (/Users/tdhcuong/Desktop/Personal_Projects/Python_Live_Coding):

**backend/requirements.txt** — exact content:
```
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
```

**backend/app/__init__.py** — empty file (marks app as a package).

**backend/app/main.py** — FastAPI application entry point:
```python
from fastapi import FastAPI
from fastapi.middleware.cors import CORSMiddleware

app = FastAPI(title="Python Live Coding")

app.add_middleware(
    CORSMiddleware,
    allow_origins=["http://localhost:5173", "http://127.0.0.1:5173"],
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)


@app.get("/")
async def health_check():
    return {"status": "ok"}
```

This sets up CORS to allow the Vite dev server on :5173 to call the FastAPI backend on :8000. No other logic yet.
  </action>
  <verify>
    <automated>cd /Users/tdhcuong/Desktop/Personal_Projects/Python_Live_Coding/backend && pip install -r requirements.txt -q && python -c "from app.main import app; print('FastAPI app imported OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `backend/requirements.txt` exists and contains the string `fastapi`
    - `backend/app/__init__.py` exists (may be empty)
    - `backend/app/main.py` exists and contains `from fastapi import FastAPI`
    - `backend/app/main.py` contains `CORSMiddleware`
    - `backend/app/main.py` contains `async def health_check`
    - `python -c "from app.main import app"` runs without ImportError from the backend/ directory
  </acceptance_criteria>
  <done>Backend directory exists with requirements.txt, app/__init__.py, and app/main.py. `pip install -r requirements.txt` completes without error. `python -c "from app.main import app"` succeeds from backend/ directory.</done>
</task>

<task type="auto">
  <name>Task 2: Frontend scaffold — Vite + Tailwind CSS v4 with dark theme shell</name>
  <files>frontend/package.json, frontend/vite.config.js, frontend/index.html, frontend/src/main.js, frontend/src/style.css</files>
  <read_first>
    - .planning/research/STACK.md (Tailwind v4 setup, Vite config, @tailwindcss/vite plugin)
  </read_first>
  <action>
Create the frontend project from scratch at /Users/tdhcuong/Desktop/Personal_Projects/Python_Live_Coding/frontend/.

**frontend/package.json**:
```json
{
  "name": "python-live-coding-frontend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.0.0",
    "tailwindcss": "^4.0.0",
    "vite": "^5.0.0"
  }
}
```

**frontend/vite.config.js** — Tailwind v4 first-party Vite plugin (zero config, no tailwind.config.js needed):
```javascript
import tailwindcss from "@tailwindcss/vite";

export default {
  plugins: [tailwindcss()],
  server: {
    port: 5173,
  },
};
```

**frontend/index.html** — entry HTML file with dark background, imports main.js:
```html
<!DOCTYPE html>
<html lang="en" class="dark">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Python Live Coding</title>
    <link rel="icon" type="image/svg+xml" href="/vite.svg" />
  </head>
  <body class="bg-gray-950 text-gray-100 min-h-screen">
    <div id="app"></div>
    <script type="module" src="/src/main.js"></script>
  </body>
</html>
```

**frontend/src/style.css** — Tailwind v4 import (this replaces tailwind.config.js in v4):
```css
@import "tailwindcss";

:root {
  --color-bg: #030712;
  --color-surface: #111827;
  --color-border: #1f2937;
  --color-accent: #6366f1;
  --color-accent-hover: #4f46e5;
  --color-text-primary: #f9fafb;
  --color-text-secondary: #9ca3af;
  --color-success: #22c55e;
  --color-error: #ef4444;
}

body {
  background-color: var(--color-bg);
  color: var(--color-text-primary);
  font-family: 'Inter', system-ui, -apple-system, sans-serif;
}
```

**frontend/src/main.js** — imports styles, renders a placeholder:
```javascript
import "./style.css";

document.getElementById("app").innerHTML = `
  <div class="flex items-center justify-center min-h-screen">
    <div class="text-center">
      <h1 class="text-3xl font-bold text-gray-100 mb-2">Python Live Coding</h1>
      <p class="text-gray-400">Loading...</p>
    </div>
  </div>
`;
```

After creating the files, run `npm install` from frontend/ to install dependencies.
  </action>
  <verify>
    <automated>cd /Users/tdhcuong/Desktop/Personal_Projects/Python_Live_Coding/frontend && npm install --silent 2>&1 | tail -3 && node -e "const fs=require('fs'); const cfg=fs.readFileSync('vite.config.js','utf8'); if(!cfg.includes('@tailwindcss/vite')) throw new Error('missing tailwindcss plugin'); console.log('vite.config.js OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/package.json` exists and contains the string `@tailwindcss/vite`
    - `frontend/vite.config.js` exists and contains `tailwindcss()` in the plugins array
    - `frontend/index.html` exists and contains `src="/src/main.js"`
    - `frontend/src/style.css` exists and contains `@import "tailwindcss"`
    - `frontend/src/main.js` exists and contains `import "./style.css"`
    - `frontend/node_modules/` directory exists after `npm install`
    - `frontend/node_modules/vite` directory exists
    - `frontend/node_modules/tailwindcss` directory exists
  </acceptance_criteria>
  <done>All frontend files created. `npm install` completes without error. node_modules contains vite and tailwindcss packages.</done>
</task>

</tasks>

<verification>
From project root:

```bash
# Backend: verify imports work
cd /Users/tdhcuong/Desktop/Personal_Projects/Python_Live_Coding/backend && python -c "from app.main import app; print(app.title)"

# Frontend: verify node_modules installed
ls /Users/tdhcuong/Desktop/Personal_Projects/Python_Live_Coding/frontend/node_modules/vite

# Frontend: verify Tailwind config
grep -n "tailwindcss" /Users/tdhcuong/Desktop/Personal_Projects/Python_Live_Coding/frontend/vite.config.js

# Frontend: verify CSS import
grep -n "@import" /Users/tdhcuong/Desktop/Personal_Projects/Python_Live_Coding/frontend/src/style.css
```
</verification>

<success_criteria>
- `backend/requirements.txt`, `backend/app/__init__.py`, `backend/app/main.py` all exist
- `python -c "from app.main import app"` succeeds (run from backend/)
- `frontend/package.json`, `frontend/vite.config.js`, `frontend/index.html`, `frontend/src/main.js`, `frontend/src/style.css` all exist
- `frontend/node_modules/` exists with vite and tailwindcss installed
- No import errors, no missing files
</success_criteria>

<output>
After completion, create `.planning/phases/01-room-infrastructure/01-01-SUMMARY.md`
</output>
